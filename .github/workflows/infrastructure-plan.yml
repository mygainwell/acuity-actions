name: Infrastructure Plan

on:
  push:
    paths:
      - 'infrastructure-plan/**'
      - '.github/actions/infrastructure-plan/**'
      - '.github/workflows/infrastructure-plan/**'
  pull_request:
    branches:
      - main

jobs:
  mock-data-dev:
    runs-on: [self-hosted]
    strategy:
        fail-fast: false
    steps:
      - name: "Setup - Checkout"
        uses: actions/checkout@v2.1.0
      - name: "Infrastructure Plan"
        uses: ./.github/actions/infrastructure-plan
        with:
          infrastructure-path: infrastructure-plan
